<%= form_with(model: project, local: true) do |form| %>
  <div class="field">
    <%= form.text_field :name, class: "name", placeholder: "Project Name" %>
  </div>
  <%= form.hidden_field :user_id, :value => current_user.id %>
<% end %>
